
services:

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sunfish-backend
    volumes:
      - .:/app
    restart: unless-stopped
    tty: true
    privileged: true
